# UPAY Frontend

Interfaz web de UPAY construida con React, Vite, Tailwind CSS, React Router, Framer Motion, Lucide React y Axios.

## Comandos

```bash
npm install
npm run dev
```

## API Gateway

Crea un archivo `.env` basado en `.env.example`:

```bash
VITE_API_URL=http://localhost:3000
```
